apiVersion: v1
kind: Service
metadata:
  name: microservice-a
  namespace: microservices
spec:
  type: LoadBalancer
  selector:
    app: microservice-a
  ports:
    - port: 8080
      targetPort: 8080